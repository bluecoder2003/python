#with numpy

import numpy as np
n=int(input("enter number of elements"))
elements=np.empty(n)
#for i in range(n):
    #elements[i] = (input("Enter elements"))
mean=np.mean(elements)
median=np.median(elements)
print(mean)
print(median)

#simple


